Semantic checks for:
    required settings
    not-allowed settings

    Added mechanism. Need to add invokations

    Need either/or

Visitor
    Semantics?

Test suite
Command line options
    ast
        xmllint output

grammar code snippets for subsystem
code gen for subsystem
     system: step()     xxx
    _Impl: update()     xxx
    log
        AddItem(&long, string format);
        Output()
    csv subclass log. Override Output()

    main
        vars: start, end
        log, csv = nullptr;
        objects: systems
        log: create log
            add items
        csv: create csv
            add items

        for start .. end
            obj1.step()
            obj2.step()
            log->Output(index)
            csv->Output(index)

allow multiple logs and csv's
add name to LOG, CSV
